import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.footer.accessibility',
  appName: 'cs-footer-accessibility',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
